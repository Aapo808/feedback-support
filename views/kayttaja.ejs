<!DOCTYPE html>
<html>
<head>
    <title><%= user.name %> - Käyttäjän tiedot</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Käyttäjän <%= user.name %> tiedot</h1>

        <form action="/users/<%= user.id %>" method="POST">
            <div class="form-group">
                <label for="company">Asiakas</label>
                <input type="text" id="company" name="company" value="<%= user.company %>" required>
            </div>

            <div class="form-group">
                <label for="name">Nimi</label>
                <input type="text" id="name" name="name" value="<%= user.name %>" required>
            </div>

            <div class="form-group">
                <label for="email">Sähköposti</label>
                <input type="email" id="email" name="email" value="<%= user.email %>" required>
            </div>

            <div class="form-group">
                <label for="password">Salasana (jätä tyhjäksi jos et halua muuttaa)</label>
                <input type="password" id="password" name="password">
            </div>

            <div class="form-group">
                <label for="admin">Admin-oikeudet</label>
                <input type="checkbox" id="admin" name="admin" <%= user.admin === 1 ? 'checked' : '' %>>
            </div>

            <button type="submit" class="btn">Tallenna muutokset</button>
            <a href="/users" class="btn btn-secondary">Takaisin käyttäjälistaan</a>
        </form>
    </div>
</body>
</html>